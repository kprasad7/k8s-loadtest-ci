name: Load Test CI

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  load-test:
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Install kind
        run: |
          curl -Lo kind https://kind.sigs.k8s.io/dl/v0.22.0/kind-linux-amd64
          chmod +x kind
          sudo mv kind /usr/local/bin/kind

      - name: Install kubectl
        run: |
          curl -Lo kubectl https://dl.k8s.io/release/v1.29.2/bin/linux/amd64/kubectl
          chmod +x kubectl
          sudo mv kubectl /usr/local/bin/kubectl

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Cypress
        run: npm install -g cypress

      - name: Validate environment
        run: python scripts/validate.py

      - name: Run tests
        run: python scripts/test.py

      - name: Code quality scan with Semgrep
        id: semgrep_scan
        uses: returntocorp/semgrep-action@v1
        with:
          generateSarif: true
          config: >-
            p/python
            p/security-audit
            p/owasp-top-ten

      - name: Upload Semgrep results to GitHub Security
        if: ${{ always() && steps.semgrep_scan.outputs.sarif != '' }}
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: ${{ steps.semgrep_scan.outputs.sarif }}
          category: 'semgrep'

      - name: Create KinD cluster
        run: python scripts/create_cluster.py

      - name: Deploy workloads
        run: python scripts/deploy.py

      - name: Check workload health
        run: python scripts/check_health.py

      - name: Run Cypress E2E tests
        id: cypress
        run: python scripts/run_cypress_tests.py
        continue-on-error: true

      - name: Run load test
        run: python scripts/load_test.py

      - name: Monitor resource utilization
        run: python scripts/monitor_resources.py --duration 30 --interval 5

      - name: Post PR comment
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python scripts/post_comment.py

      - name: Security scan with Trivy
        id: trivy_scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'
          format: 'sarif'
          output: 'trivy-results.sarif'

      - name: Upload Trivy results to GitHub Security
        if: ${{ always() && steps.trivy_scan.outputs.sarif != '' }}
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: ${{ steps.trivy_scan.outputs.sarif }}

      - name: Delete KinD cluster
        if: always()
        run: python scripts/delete_cluster.py

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: load-test-artifacts
          path: artifacts/
